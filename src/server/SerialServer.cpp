#include "SerialServer.hpp"

namespace server{

    void SerialServer::open(uint32_t serverPort, const clientside::SolverClientHandler<searcher::Graph, searcher::SearchResult>& clientHandler) const{
        
    }

    void SerialServer::stop() const{

    }

}