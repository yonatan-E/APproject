#pragma once

#include "Searcher.hpp"
#include <string>

namespace searcher{

    template <typename IdentifierType, typename ValueType>
    std::vector<std::string> Searcher::search(const Searchable<IdentifierType, ValueType>& searchable) const {

    }
}